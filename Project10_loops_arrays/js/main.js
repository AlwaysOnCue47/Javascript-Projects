// Loops array
function callLoop() {
  X = 0;
  list = ["apples", "Oranges", "Bananas", "pears","Pineaplle"];
  Y = list.length;
  while (X < Y) {
    console.log(list[X]);
    X++;
  } 
  console.log(Y);
}